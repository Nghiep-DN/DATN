@model List<onlineShopSolution.ViewModel.Catalog.Categories.CategoryViewModel>
@inject LazZiya.ExpressLocalization.ISharedCultureLocalizer _loc;
@{
    var culture = CultureInfo.CurrentCulture.Name;
}
<div id="sidebar" class="span3">
    <div class="well well-small"><a id="myCart" href="/@culture/Cart"><img src="/themes/images/ico-cart.png" alt="cart">[ <span id="numberItem-sideBar">0</span> ]  <localize>Items in your carts</localize>   <span class="badge badge-warning pull-right" id="tbl-total-sideBar"></span></a></div>
    <ul id="sideManu" class="nav nav-tabs nav-stacked">
        @{
            var parents = Model.Where(x => x.ParentId == null);
        }
        @for (int index = 0; index < parents.Count(); index++)
        {
            var parent = parents.ElementAt(index);
            var children = Model.Where(x => x.ParentId == parent.Id);
            if (children.Any())
            {
                <li class="subMenu open">
                    <a> @parent.Name</a>
                    <ul>
                        @foreach (var item in children)
                        {
                            <li><a class="active" href="/@culture/@_loc["categoryUrl"]/@item.Id"><i class="icon-chevron-right"></i>@item.Name </a></li>
                        }
                    </ul>
                </li>
            }
            else
            {
                <li><a href="/@culture/@_loc["categoryUrl"]/@parent.Id">@parent.Name</a></li>
            }

        }
    </ul>
    <br />
    <div class="thumbnail">
       
        <img src="/themes/images/products/tra_sencha_hoa_cuc__86g__hop__matchi_matcha_-_01_-_01a_compact.jpg" />
        <div class="caption">
            <h5>Trà Sencha hoa cúc</h5>
            <h4 style="text-align:center"><a class="btn" href="#"> <i class="icon-zoom-in"></i></a> <a class="btn" href="#">Thêm <i class="icon-shopping-cart"></i></a> <a class="btn btn-primary" href="#">109.000</a></h4>
        </div>
    </div><br />
          <div class="thumbnail">
              <img src="~/themes/images/products/200701_set_qua_l_angfarm_de_amour__hop__nhan_do_-_110985_-_01_f87113b283e8428baccca34ed536c82a_compact.jpg" />
              <div class="caption">
                  <h5>Sét quà tặng</h5>
                  <h4 style="text-align:center"><a class="btn" href="#"> <i class="icon-zoom-in"></i></a> <a class="btn" href="#">Thêm <i class="icon-shopping-cart"></i></a> <a class="btn btn-primary" href="#">285.000</a></h4>
              </div>
          </div><br />
        <div class="thumbnail">
            <img src="/themes/images/payment_methods.png" title="Bootshop Payment Methods" alt="Payments Methods">
            <div class="caption">
                <h5><localize>ESTIMATE</localize></h5>
            </div>
        </div>
</div>